pairs = [(1,"one"),(2,"two"),(3,"three"),(4,"four")]
pairs.sort(key = lambda pair:pair[1])
print pairs

#using python in cmd
#using ctr + d to quit

#appledembp:desktop apple$ python lambda.py 
#[(4, 'four'), (1, 'one'), (3, 'three'), (2, 'two')]